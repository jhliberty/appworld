<!DOCTYPE html>
<html>
<head>
  <%= render "layouts/header" %>
</head>
<body>
  <div class="topbar">
    <div class="fill">
      <div class="container">
        <%= link_to "App World", root_path, :class => "brand"%>
        <ul class="nav" style="float:right;">
          <% if current_user %>
          <li><%= link_to "Dashboard", dashboard_path %></li>
          <li><%= link_to "Signout", signout_path %></li>
          <% else %>
          <li><%= link_to "Signin", signin_path %></li>
          <li><%= link_to "Signup", signup_path %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>

  <div class="container">
    <div class="content">
      <% flash.each do |key, value| %>
        <div class="alert-message fade in <%= key %>" data-alert="alert"><a class="close" href="#">&times;</a><%= value %></div>
      <% end %>

      <%= yield %>
      
    </div>
    <%= render "layouts/footer" %>
    <%= debug(params) if Rails.env.development? %>
  </div> <!-- /container -->
</body>
</html>
